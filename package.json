{
  "name": "uncover",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "VITE_BASE_PATH=/Uncover2/ vite build",
    "preview": "vite preview",
    "preview:pages": "VITE_BASE_PATH=/Uncover2/ vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
